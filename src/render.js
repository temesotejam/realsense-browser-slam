export function drawDepth(canvas,depth,w,h,maxRange){if(!depth)return;const ctx=canvas.getContext('2d');const outW=w*2,outH=h*2;if(canvas.width!==outW||canvas.height!==outH){canvas.width=outW;canvas.height=outH;}const small=document.createElement('canvas');small.width=w;small.height=h;const sctx=small.getContext('2d');const img=sctx.createImageData(w,h),d=img.data;for(let i=0;i<depth.length;i++){const z=depth[i];let c=[4,7,10];if(z>0)c=palette(Math.min(1,z/maxRange));const j=i*4;d[j]=c[0];d[j+1]=c[1];d[j+2]=c[2];d[j+3]=255;}sctx.putImageData(img,0,0);ctx.imageSmoothingEnabled=false;ctx.drawImage(small,0,0,outW,outH);}
function palette(t){const a=[[32,8,76],[39,63,181],[18,166,197],[68,218,116],[237,217,72],[244,88,48]],p=[0,.2,.42,.64,.82,1];let k=0;while(k<p.length-2&&t>p[k+1])k++;const f=Math.max(0,Math.min(1,(t-p[k])/(p[k+1]-p[k])));return a[k].map((v,i)=>Math.round(v+(a[k+1][i]-v)*f));}
export class TrajectoryRenderer{constructor(canvas){this.c=canvas;this.ctx=canvas.getContext('2d');this.scale=100;this.cx=0;this.cz=0;canvas.addEventListener('wheel',e=>{e.preventDefault();this.scale=Math.max(20,Math.min(900,this.scale*Math.exp(-e.deltaY*.001)));},{passive:false});}render(path,keyframes=[]){const c=this.c,ctx=this.ctx,W=c.width,H=c.height;ctx.fillStyle='#05080b';ctx.fillRect(0,0,W,H);ctx.strokeStyle='#17212b';ctx.lineWidth=1;const s=Math.max(30,this.scale*.5);for(let x=W/2%s;x<W;x+=s){ctx.beginPath();ctx.moveTo(x,0);ctx.lineTo(x,H);ctx.stroke();}for(let y=H/2%s;y<H;y+=s){ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(W,y);ctx.stroke();}const proj=p=>[W/2+(p.x-this.cx)*this.scale,H/2-(p.z-this.cz)*this.scale];if(path.length){const last=path[path.length-1];this.cx=this.cx*.96+last.x*.04;this.cz=this.cz*.96+last.z*.04;ctx.strokeStyle='#f0c36a';ctx.lineWidth=2;ctx.beginPath();path.forEach((p,i)=>{const q=proj(p);i?ctx.lineTo(q[0],q[1]):ctx.moveTo(q[0],q[1]);});ctx.stroke();ctx.fillStyle='#58a6ff';const q=proj(last);ctx.beginPath();ctx.arc(q[0],q[1],5,0,Math.PI*2);ctx.fill();}ctx.fillStyle='#3fb950';for(const p of keyframes){const q=proj(p);ctx.fillRect(q[0]-2,q[1]-2,4,4);}}}
